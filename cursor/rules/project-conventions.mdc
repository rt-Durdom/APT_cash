---
description: Базовые правила для проекта APT_cash и похожих проектов
alwaysApply: true
---

# Общие установки для APT_cash

- При работе с этим репозиторием ИИ должен:
  - Учитывать наличие папки с похожими проектами: `/Users/romanturskov/Documents/Learn_Python/Тестовые_работа/Proninteam_test/projects`.
  - При необходимости примеров или типовых решений может просматривать код в этих проектах (только на чтение).
  - Использовать их как источник идей и паттернов, но не копировать слепо, а адаптировать под текущий проект.
  - Должно быть выполнено минимум 2 итерации проверки, и только после этого выдавать код Пользователю.

- Текстовое описание текущего проекта:
  - Основное текстовое описание и требования к проекту располагать в файле `PROJECT_SPEC.md` в папке `/Users/romanturskov/Documents/Learn_Python/Тестовые_работа/Proninteam_test/.cursor/rules/`.
  - Перед началом любой существенной разработки ИИ должен прочитать `PROJECT_SPEC.md` и опираться на него при принятии архитектурных и кодовых решений.

- Порядок принятия решений:
  1. Сначала опираться на явные требования из `PROJECT_SPEC.md`.
  2. Затем смотреть на реализованные решения и стиль внутри текущего репозитория.
  3. При необходимости — заглядывать в проекты из папки `/Users/romanturskov/Documents/Learn_Python/Тестовые_работа/Proninteam_test/projects` для вдохновения и единообразия.
  4. Если что‑то не определено правилами и примерами — предлагать аккуратное, понятное и расширяемое решение по умолчанию.

# Генерирование кода
- ИИ только предлагает свои варианты кода. Пользователь сам принимает решение об его использовании и сам вставляет в проект.
- НЕ вносить изменения в код (в любые файлы проекта) БЕЗ явного разрешения пользователя.
- Изменения в код может вносить ТОЛЬКО пользователь.
- Показывать пояснения, примеры кода и инструкции
- Указывать конкретные места в коде (файл, строка) где нужно внести изменения
- Объяснять, для чего нужен код и где он применяется

## Стиль общения
- Отвечать на русском языке
- Пояснять код подробно: для чего нужен, где применяется
- Указывать конкретные места изменений маркерами ("📍 МЕСТО 1: ...")
- Объяснять связь с предыдущими модулями и задачами
- Показывать ссылку на документацию, чтобы можно было посмотреть её и изучить/использовать

# Этапность выполнения проекта
Этап 1. Инициализация проекта и структуры
Цель: пустой, но корректно настроенный Django/DRF‑проект в Docker.
Содержимое:
docker-compose.yml (PostgreSQL + web).
Dockerfile для Django.
Виртуальное окружение UV
Создан проект Django и базовое приложение (например, core или collects).
Базовые настройки: INSTALLED_APPS, подключение DRF, настройки БД из переменных окружения.
Ветка: feature/01-init-project-and-docker.

Этап 2. Описание моделей домена
Цель: описать модели и связи без сложной логики.
Содержимое:
Модель User (скорее всего, стандартная auth.User или кастомная).
Модель Collect со всеми полями из спецификации.
Модель Payment (привязка к Collect, сумма, дата/время, пользователь).
Миграции.
Ветка: feature/02-models.

Этап 3. Подключение БД и запуск через Docker
(частично у тебя уже в п.3, я чуть подробнее разложу)
Цель: рабочий проект, который стартует через docker compose up, даёт миграции и подключается к PostgreSQL.
Содержимое:
docker-compose.yml (PostgreSQL + web).
Dockerfile для Django.
Проверка/настройка docker-compose.yml (volume для данных, порты, env).
Команда запуска (например, в docker-compose: python manage.py migrate && gunicorn ... или пока runserver).
Документация в README/PROJECT_SPEC с командами запуска.
Ветка: feature/03-db-and-docker.

Этап 4. CRUD API для Collect и Payment
Цель: рабочий REST API для основных сущностей.
Содержимое:
Сериализаторы для Collect и Payment.
ViewSet’ы / Generic views DRF.
URL‑маршруты.
Базовая проверка прав (например, автор может редактировать свой сбор и т.п.).
Ветка: feature/04-crud-api.

Этап 5. Отправка e‑mail при создании сбора/платежа
Цель: уведомления по почте работают.
Содержимое:
Настройки e‑mail (простые, например, через консольный backend на время разработки или реальный SMTP).
Сигналы или сервис-слой, который при создании Collect / Payment отправляет письмо автору/донатору.
Ветка: feature/05-email-notifications.

Этап 6. Кэширование GET‑эндпоинтов
Цель: добавить кэш и обеспечить корректное обновление.
Содержимое:
Настройка backend-а кэша (например, Redis) в Docker.
Кэширование списков и деталей Collect и/или других нужных эндпоинтов.
Инвалидация кэша при изменении/создании/удалении данных.
Ветка: feature/06-cache.

Этап 7. Swagger/ОpenAPI документация
Цель: документированный API.
Содержимое:
Подключение drf-yasg или drf-spectacular.
Эндпоинт /swagger или /api/schema/swagger-ui/.
Описание основных схем и примеров.
Ветка: feature/07-swagger-docs.

Этап 8. Management‑команда для мок‑данных
Цель: быстрая генерация тестовых данных.
Содержимое:
Management команда (например, python manage.py seed_data).
Генерация нескольких тысяч пользователей, сборов и платежей с реалистичными полями.
Ветка: feature/08-management-command.

Этап 9. Тесты и финальная полировка
Цель: минимальный набор автотестов и приведение всёго в порядок.
Содержимое:
Тесты на модели и ключевые эндпоинты.
Проверка запуска через Docker.
Обновление README с инструкциями по запуску.
Ветка: feature/09-tests-and-polish.

